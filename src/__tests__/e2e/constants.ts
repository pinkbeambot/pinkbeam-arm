/**
 * Shared E2E test constants
 * 
 * This file contains constants shared between the auth setup and test fixtures.
 * It avoids circular dependencies between test files.
 */

export const TEST_EMAIL = 'e2e-test@pinkbeam-test.com';
export const STORAGE_STATE = '.playwright/.auth/user.json';
